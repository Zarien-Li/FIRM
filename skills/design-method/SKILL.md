---
name: design-method
description: Cultivates a load-bearing method primitive from reproduced evidence and a design-relevant explanation, preserving useful computation across a coherent construction arc instead of restarting after every result.
when_to_use: Use after competent empirical contact when inventing or repairing an architecture, objective, state, memory, routing, training, data, or systems primitive.
argument-hint: "[evidence, failure, or design target]"
---

# Method Primitive Synthesis

Build a positive design object that makes the desired behavior natural by design.
Method formation is a sustained construction arc: implementation, failure, ablation,
repair, simplification, and comparison under one evolving principle.

Aim for a reusable primitive rather than one more model instance. Research may be
complex; the final intellectual object should be as simple as the evidence permits.

## Enter From A Valuable Design Opportunity

Method work may begin from:

1. an independently important known problem with a consequential incumbent
   compromise; or
2. a reproduced natural failure whose evidence implicates a changeable design
   assumption.

Before serious construction, know:

- the value-bearing natural population and accepted outcome;
- a competent substrate on the standard task;
- a serious incumbent and strong simple alternative;
- the nearest two or three same-locus methods and the strongest ownership threat;
- the discovery slice, intended reach, material scope debt, and return to the accepted
  surface;
- live explanations and inconvenient evidence;
- lessons inherited from earlier realizations.

The closest rival should be run or deeply inspected before novelty lock and dependent
optimization. An unresolved rival does not forbid exploratory construction, but it
keeps novelty and paper maturity provisional.

A fashionable operator, formula, oracle, clean probe, or positive private cell is not
enough. Do not choose a dataset or regime because it makes the candidate work.

## Candidate, Realization, And Paper-Bearing Method

A **candidate** may be a formula, name, sketch, objective, probe, CPU audit, oracle,
smoke test, or pre-check. It is not yet a method.

A **realization** exists when:

- a trainable or executable implementation runs on the accepted task;
- the intended load-bearing computation demonstrably activates;
- the substrate is competent enough to interpret the result;
- end-to-end behavior is compared under a meaningful matched treatment.

A **paper-bearing method** additionally has a healthy comparison with the most
claim-threatening rival, understood load-bearing component or interaction, relevant
utility and tradeoffs, and a stable bounded claim.

Do not use a method name, diagram, code path, pilot, or coherent story as a maturity
substitute.

## Propose A Principle And A Construction

The lead PI owns the first construction and selects it from local evidence. After
competent contact has earned a real design opportunity, the PI may use Gemini as a
creative co-PI to challenge the default assumption and co-invent a load-bearing
primitive. Codex may challenge or repair a supported construction later; it must not
generate the project's first method from an underdetermined observation.

For a serious candidate state:

- observations and contrary cases it compresses;
- the consequential default assumption it changes;
- the one-sentence replacement principle and method;
- load-bearing component or interaction and unique behavioral prediction;
- difference from the nearest rival and strongest simple composition;
- attainable end-to-end value and expected capability or cost tradeoff;
- what the smallest real implementation teaches even if it loses;
- adoption path and where the same principle should or should not transfer.

Possible loci include representation/state, update/read rules, routing, memory,
objective factorization, credit assignment, training interaction, curriculum, data
treatment, and execution semantics.

Apply the **80% deletion test** throughout construction: remove auxiliary modules,
dataset-specific vocabulary, scaffolding, and defensive additions. Identify the
irreducible new rule or computation. Do not remove components that evidence shows are
causally necessary.

Prompts, thresholds, calibration, certificates, probes, verifiers, abstention, and
wrappers may be baselines, components, or contributions. Judge whether they embody an
irreducible principle with real value, not their surface form.

Select one provisional construction rather than returning an unranked menu. Compare
genuinely distinct candidates only when the evidence supports them.

When a creative outside view can change the design rather than merely add variants,
give Gemini the actual natural evidence, contrary cases, nearest rivals, constraints,
and unresolved design question. Let the PI author the prompt for the episode; do not
impose a fixed output template. Follow `/firm:second-pi` for the verified invocation
and model pin. Treat the response as a proposal: collision-read it, choose one
principle, implement the smallest real realization, and let evidence reshape it.

Gemini performs divergence, not selection. The lead PI must compress the response into
at most one selected or synthesized principle before it can affect a construction arc.
Keep unused proposals in a separate creative note; do not create parallel tasks,
experiments, method names, or live-state branches from them. Codex challenges the
PI-selected construction after this synthesis rather than screening the raw brainstorm.

## Cultivate One Construction Arc

A construction arc keeps one central design question and may contain several versions.
Do not rename each local change as a new method or send every result through a new
review cycle.

The arc should eventually establish:

1. **real implementation:** the intended computation runs on the accepted task;
2. **constructive comparison:** a component is added, removed, replaced, frozen,
   rerouted, or factorized so that the next design is learned from the contrast;
3. **paired utility:** capability, coverage, latency, memory, compute, or another cost
   relevant to the intended value;
4. **decisive comparison:** the strongest simple alternative and claim-threatening
   rival when the method is ready to support a claim.

These are the arc's scientific obligations, not a demand that every early version run
the full paper table. Check utility early when it is a central design risk; otherwise
complete it before treating the realization as positive and scalable.

Within the arc:

- smoke and engagement checks establish plumbing;
- development evidence may select and reshape the design;
- compact factorials or substitutions are preferred when components compensate or
  interact;
- fresh evidence later confirms paper-carrying necessity or interaction claims;
- matched information, supervision, capacity, optimization, and evaluation identify
  what the construction actually contributes.

An operational FIRM construction lease may protect the arc from unrelated injection.
Its marker and lifecycle belong to project operations. It may span evidence-directed
v1 -> v2 repairs under the same central principle; it must not turn each version into
a scientific gate. Release it when the arc's question is answered, invalidated, or
made irrelevant.

Use `/firm:second-pi` Method Challenge once the PI has a supported construction and a
coherent arc proposal. Do not call it after every component output.

## Repair From Evidence

After a realization misses its target, locate the earliest unsupported link:

- non-engagement or implementation defect;
- optimization or integration failure;
- component acted but solved the wrong proxy;
- useful pathway coupled to a regression;
- a natural context variable incorrectly fixed;
- decisive alternative made the construction redundant.

Preserve the honest verdict and record:

- activated computation;
- useful residual;
- failed coupling or assumption;
- inherited change and prospective prediction for the next version.

Classify the next change:

- **correction:** verified implementation, evaluator, optimizer, or integration repair;
- **construction:** changed load-bearing component or interaction with a new prediction;
- **tuning:** seed, coefficient, threshold, schedule, dose, or nearby treatment only.

A verified correction normally earns one clean matched rerun. A continuing directional
loss is not repaired by a new nickname or seed sweep. A further construction needs a
changed design question supported by evidence.

After an informative failed realization has identified the activated computation,
useful residual, and failed coupling, Gemini may help invent a genuinely different v2
primitive. If the whole arc instead shows frame lock -- more local variants and richer
diagnosis without a new editable computation -- do not manufacture another same-frame
v3. Invite a creative challenge to the default assumption or re-ground the program.
Do not call Gemini merely because a score is negative or before implementation
competence is established.

One failed realization does not close a primitive, family, problem, or field. Several
failures do not automatically justify a higher abstraction. Before leaving the current
arc, inspect substrate competence, problem value, nearest-rival ownership, and the
method assumption itself.

Continue the arc while evidence is teaching a genuinely new component job or
interaction and the natural problem remains valuable. Return to the Program Compass
when another version would merely repeat an answered prediction, elaborate a private
cell, or preserve activity without plausible accepted-task consequence. There is no
fixed failure count.

## Learn From Heterogeneity And Simple Baselines

When effects vary by model, dataset, scale, query, or system state, ask whether a
load-bearing context variable should be represented. An adaptive method is justified
only when the variable predicts variation beyond dataset names and beats strong fixed
or simple conditional alternatives.

When a simple rule, solver, retrieval system, cache, merge, or calibration wins,
explain the computation, natural boundary, and cost. A native, learned, differentiable,
efficient, or robust construction is worthwhile only when it addresses a remaining
non-redundant need.

## Expand A Credible Primitive

After a realization survives its component comparison and relevant utility check,
consider whether the principle supports:

- another accepted task or natural system where the same assumption recurs;
- reusable supervision, data construction, training semantics, or infrastructure;
- model-family, scale, modality, or generation predictions;
- a named phenomenon with independent definition and consequence;
- a simpler or lower-friction realization.

Use `/firm:second-pi` for an independent critical or creative Program Expansion view
before paper-sized investment. Gemini may propose a larger scientific form of the same
positive primitive; Codex may attack its value, ownership, and evidence. Choose
extensions predicted by the principle, not favorable extra cells or table breadth.

## Durable Construction Note

Update the live state directly for a compact arc. Create `METHOD_PRIMITIVE.md` only
when the design needs durable multi-actor coordination:

```markdown
# Method Primitive
## Research Basis
- natural problem, accepted surface, substrate, rivals, and contrary evidence:
## Principle
- default assumption, one-sentence replacement, value, and adoption path:
## Construction Arc
- central design question and unique prediction:
- current realization and component jobs:
- versions: activated computation | useful residual | failed coupling | inheritance:
- utility risk and decisive comparison:
## Expansion
- earned and unsupported axes:
## Next Action
- next implementation or comparison and what it teaches:
```

This note records an evolving scientific object. It does not grant paper maturity.
